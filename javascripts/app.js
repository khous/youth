$.ready(function () {
/*
    series browser, lesson browser (DDL and cursors)
    flow, get library.json
    check for last lesson accessed in local storage
    else
    get current date, find the current lesson, or nearest lesson defaulting to the last lesson
*/
    //SHOW load mask
    //make the UI

});